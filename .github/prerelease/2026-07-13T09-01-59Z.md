<!-- prerelease: timestamp=2026-07-13T09-01-59Z repo=danieljustus/symaira-desktop head=c8850fc8d2e35e0a5d1b8104e3008d9fffbb504a target=v0.6.10 bump=patch gate=pass -->

# Prerelease Gate — 2026-07-13

## Summary
- Repo: danieljustus/symaira-desktop
- HEAD: c8850fc8d2e35e0a5d1b8104e3008d9fffbb504a
- Last release: v0.6.9
- Proposed target: v0.6.10 (bump: patch)
- Gate: pass

## Gate checks
| Check | Result | Detail |
|---|---|---|
| CI / tests (HEAD) | PASS | GitHub Actions CI green on c8850fc (test ubuntu/macos, lint, Analyze/CodeQL all success). Local `go test ./internal/service` fails reproducibly on this machine only (search-index cross-test pollution) — reproduces identically on pre-release `main`, not a HEAD-specific regression; fresh CI runners are clean. |
| Version consistency | PASS | 3 mentions checked (`cmd/symdesk/main.go`, `internal/mcp/server.go`, `project.yml`), all 3 auto-corrected `0.6.9` → `0.6.10` (working tree, unpushed); `SymDesk.xcodeproj/project.pbxproj` regenerated via `xcodegen` to match. No README/docs version mentions found. |
| README / doc currency | PASS | Diff since v0.6.9 touches no `*.md` files; no user-facing feature changes to document (release-workflow fix + PDF/export test coverage only). |
| Coverage vs threshold | WARN | overall 72.1% vs 80% default (no repo-configured gate — informational only, pre-existing repo-wide state, not introduced by this release) |
| Coverage regression | PASS | baseline 70.6% (v0.6.9) → 72.1% (HEAD); delta +1.5% |
| Patch coverage | PASS | n/a — no non-trivial production-code lines in the diff (only two trivial version-string literals plus pure test additions) |
| Assets preview | PASS | goreleaser matrix (`symaira-desktop_<ver>_{linux,darwin,windows}_{amd64,arm64}`, `checksums.txt`) + signed/notarized `SymDesk-<ver>.{zip,dmg}` + Homebrew cask bump, all producible by the existing, already-verified `.github/workflows/release.yml` (confirmed working end-to-end for v0.6.9 in PR #139's fix). |
| Security | PASS | 0 open code-scanning alerts, 0 open high/critical Dependabot alerts, 0 open secret-scanning alerts. |

## Findings
- [ ] **[WARN][Coverage] Overall statement coverage below the informal 80% default bar**
  - **Status quo:** `go test -coverprofile` reports 72.1% overall statements; no repo-configured coverage gate exists in CI.
  - **Likely cause:** Pre-existing repo-wide state — not introduced by the two commits since v0.6.9 (which raised coverage +1.5%, see the regression check above).
  - **Proposed solution:** Not release-blocking. If desired, run `/test-coverage` (full, non-baseline-scoped) in a normal iteration and hand off via `/02-gh-plan --from-coverage`.
  - **Handoff:** `/test-coverage` → `/02-gh-plan --from-coverage <path>` → `/03-gh-go`

## Applied mechanical fixes (default mode)
- `cmd/symdesk/main.go:18` `var version = "0.6.9"` → `"0.6.10"`
- `internal/mcp/server.go:18` `var ServerVersion = "0.6.9"` → `"0.6.10"`
- `project.yml:62` `MARKETING_VERSION: "0.6.9"` → `"0.6.10"`
- `SymDesk.xcodeproj/project.pbxproj` regenerated via `xcodegen generate --spec project.yml` to propagate the `MARKETING_VERSION` bump into the 2 build-config entries.

These edits are unpushed in the working tree and are intended to ride into `07-gh-release`'s release commit.

## Next step
- gate=pass  → `/07-gh-release`
